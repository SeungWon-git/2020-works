1. 비주얼 업데이트로 빌드오류(internal error during image buildimage)가 발생할 수 있는데, 
이거 그냥 x64,Realese파일들 삭제하고 다시 빌드하면 실행 잘됨(~7주차 과제까지는 업뎃함)

2. ReadObj함수에서 vn,vt까지 받는다면 읽어드리는 obj파일의 f 부분이 f 1/2/3 ... 이런식으로 
있어야지 오류가 안생김 (%f/%f/%f 형식으로 읽어 드리므로 그럼) 

3. 비트수준 24->32/32->24 만드는 법: 투명한 이미지효과(비트수준: RGBA가 아닌 경우 -> RGB일때)가 없는 거라면 
그냥 비주얼 솔루션으로 이미지 파일 드래그하면 이미지 편집기가 나오는데 
거기서 속성-형식 을 바꿔 주고 저장하면 됨 (24bpp BGR/32bpp BGRA)
===> [error] 24비트는 즉 RGB값만 가진 파일은 제대로 텍스처가 안 나옴 (10-1실행시켜봄 앎)
		이상하게 삐뚤어져서 나오고 색깔도 안나오고 깨짐
4. exe파일 단독 실행시키고싶다면 같은 위치에 프레그먼트 파일하고 버텍스 파일 복사해주고, res파일들(obj,png)이 있다면 res파일도 같이 위치해줘야함

5. 투명객체를 그릴때는 먼저 그릴 시  뒤에 객체가 아예 안보이게 되므로 투명객체는 제일 나중에 그려줘야함