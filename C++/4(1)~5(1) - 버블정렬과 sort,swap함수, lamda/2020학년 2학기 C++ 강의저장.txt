


----------------------------------------------
소스.cpp - 2020-09-21 오후 10:59:10 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
//변수값의 교환과 레퍼런스
// *(포인터)는 왜 사용하지?		=> 메모리를 원격(다른 저장공간에서)조작(읽고 쓰기)하려고		!!!{하지만 C++은 안쓴다!}
// &(레퍼런스)는 왜 사용하지?	=> 객체를 복사하지않고!!! 변경이 가능하다!
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 };

	//함수 호출
	f(a, sizeof(a) / sizeof(int));

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int a[], int b)
{
	for (int i = b; i > 0; --i)
		a[i - 1] = a[i - 1] * 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:08:57 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
//변수값의 교환과 레퍼런스
// *(포인터)는 왜 사용하지?		=> 메모리를 원격(다른 저장공간에서)조작(읽고 쓰기)하려고		!!!{하지만 C++은 안쓴다!}
// &(레퍼런스)는 왜 사용하지?	=> 객체를 복사하지않고!!! 변경이 가능하다!
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 };

	//함수 호출
	f(a, sizeof(a) / sizeof(int));

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int a[], int b)
{
	for (int i = b; i > 0; --i)
		a[i - 1] = a[i - 1] * 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:15:06 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; //배열은 a처럼 배열의 이름이 주소를 불러준다.

	cout << "배열의 크기" << sizeof(a) << endl;

	//함수 호출
	void f(int[]);	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int x[])
{
	cout << "넘겨받은 배열의 크기" << sizeof(x) << endl;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:16:57 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; //배열은 a처럼 배열의 이름이 주소를 불러준다.

	cout << "배열의 크기" << sizeof(a) << endl;

	//함수 호출
	void f(int[]);	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int x[])
{
	cout << "넘겨받은 배열의 크기" << sizeof(x) << endl;	//배열이 아무리커도 배열로 넘겨주면 배열의 시작지점의 주소만 받기 때문에 4라고 나오는 것임
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:18:22 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; //배열은 a처럼 배열의 이름이 주소를 불러준다.

	cout << "배열의 크기" << sizeof(a) << endl;

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //따라서 배열이 아닌 포인터로 받아야함
{
	cout << "넘겨받은 배열의 크기" << sizeof(p) << endl;	//배열이 아무리커도 배열로 넘겨주면 배열의 시작지점의 주소만 받기 때문에 4라고 나오는 것임
	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:18:46 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; //배열은 a처럼 배열의 이름이 주소를 불러준다.

	cout << "배열의 크기" << sizeof(a) << endl;

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //따라서 배열이 아닌 포인터로 받아야함
{
	cout << "넘겨받은 배열의 크기" << sizeof(*p) << endl;	//배열이 아무리커도 배열로 넘겨주면 배열의 시작지점의 주소만 받기 때문에 4라고 나오는 것임
	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:20:23 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; 

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //정의
{	
	for (int i = 0; i < 5; i++) {
		*p = *p * 2;
		++p;
	}
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:20:47 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; 

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //정의
{	
	for (int i = 0; i < 5; i++) 
		*p++ = *p * 2;
	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:21:04 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; 

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //정의
{	
	for (int i = 0; i < 5; i++) 
		*p++ *= 2;
	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:21:27 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int[], int);

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; 

	//함수 호출
	void f(int* );	//선언
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //정의
{	
	for (int i = 0; i < 5; i++) 
		p[i] *= 2;
	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:23:35 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int*);	//선언

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 }; 

	//함수 호출
	f(a);	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p) //정의
{	
	for (int i = 0; i < 5; i++) 
		p[i] *= 2;

	//p[i]라는 표현은 *(p+i)을 다르식으로 표기한것이다.
	//p[i]가 더 쉬운 표현인데 이런문법을 꿀(?)문법이라고 한다.
	// syntactic sugar (직관적 문법)	
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:28:24 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int* ,int);	//선언

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3,4,5 ,6,7}; 
	// + 배열내 인자갯수가 바뀌어도 동작하려면?

	//함수 호출
	f(a,sizeof(a)/sizeof(int));	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p,int num) //정의
{	
	for (int i = 0; i < num; i++) 
		p[i] *= 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:28:41 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int* ,int);	//선언

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3}; 
	// + 배열내 인자갯수가 바뀌어도 동작하려면?

	//함수 호출
	f(a,sizeof(a)/sizeof(int));	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p,int num) //정의
{	
	for (int i = 0; i < num; i++) 
		p[i] *= 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:30:01 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int* ,unsigned int 갯수);	//선언

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3}; 
	// + 배열내 인자갯수가 바뀌어도 동작하려면?

	//함수 호출
	f(a,sizeof(a)/sizeof(int));	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p, unsigned int num) //정의
{	
	for (int i = 0; i < num; i++) 
		p[i] *= 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:30:31 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

void f(int* ,size_t 갯수);	// unsigned int = size_t

//[다음시간문제] 함수를 호출하여 a의 값을 2배로 변경하라.
//	함수를 선언,정의,호출하는 코드를 답지에 적어라.

int main()
{
	int a[]{ 1,2,3}; 
	// + 배열내 인자갯수가 바뀌어도 동작하려면?

	//함수 호출
	f(a,sizeof(a)/sizeof(int));	//호출

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	save("소스.cpp");
}

void f(int* p, size_t num) //정의
{	
	for (int i = 0; i < num; i++) 
		p[i] *= 2;
}


----------------------------------------------
소스.cpp - 2020-09-27 오후 7:37:53 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5}; // 정렬시키자(sorting) - 1,3,5,6,7  (내림차순)
						 //						- 7,6,5,3,1	 (오름차순)
						 // IT에서 가장 많이쓰는 아주 중요한 기능

	

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:18:00 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5}; 

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	if (a[0] > a[1]) {
		int temp{ a[0] };
		a[0] = a[1];
		a[1] = temp;
	}
	if (a[1] > a[2]) {
		int temp{ a[1] };
		a[1] = a[2];
		a[2] = temp;
	}
	if (a[2] > a[3]) {
		int temp{ a[2] };
		a[2] = a[3];
		a[3] = temp;
	}
	if (a[3] > a[4]) {
		int temp{ a[3] };
		a[3] = a[4];
		a[4] = temp;
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:19:44 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5}; 

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for (int i = 0; i < num - 1; ++i) {
		if (a[i] > a[i+1]) {
			int temp{ a[i] };
			a[i] = a[i+1];
			a[i+1] = temp;
		}
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:20:09 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5,2,4,10,8,6}; 

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for (int i = 0; i < num - 1; ++i) {
		if (a[i] > a[i+1]) {
			int temp{ a[i] };
			a[i] = a[i+1];
			a[i+1] = temp;
		}
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:23:01 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5}; 

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for (int i = 0; i < num - 1; ++i) {
		if (a[i] > a[i+1]) {
			int temp{ a[i] };
			a[i] = a[i+1];
			a[i+1] = temp;
		}
	}
	for (int i = 0; i < num - 2; ++i) {
		if (a[i] > a[i + 1]) {
			int temp{ a[i] };
			a[i] = a[i + 1];
			a[i + 1] = temp;
		}
	}
	for (int i = 0; i < num - 3; ++i) {
		if (a[i] > a[i + 1]) {
			int temp{ a[i] };
			a[i] = a[i + 1];
			a[i + 1] = temp;
		}
	}
	for (int i = 0; i < num - 4; ++i) {
		if (a[i] > a[i + 1]) {
			int temp{ a[i] };
			a[i] = a[i + 1];
			a[i + 1] = temp;
		}
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:24:35 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include "save.h"	
using namespace std;

int main()
{
	int a[]{ 7,3,6,1,5}; 

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] > a[i + 1]) {
				int temp{ a[i] };
				a[i] = a[i + 1];
				a[i + 1] = temp;
			}
		}
	}


	//a는 오름차순으로 정렬되었다.
	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:28:44 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,10'0000 };

int main()
{
	int a[1000];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	/*size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] > a[i + 1]) {
				int temp{ a[i] };
				a[i] = a[i + 1];
				a[i + 1] = temp;
			}
		}
	}


	//a는 오름차순으로 정렬되었다.
	for (int n : a)
		cout << n << ' ';

	cout << endl;*/

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:29:52 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,10'0000 };

int main()
{
	int a[1000];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] > a[i + 1]) {
				int temp{ a[i] };
				a[i] = a[i + 1];
				a[i + 1] = temp;
			}
		}
	}


	//a는 오름차순으로 정렬되었다.
	cout << "정렬 후: " << endl << endl << endl;
	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:30:40 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,1000 };

int main()
{
	int a[1000];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	// [문제] a의 값중에 가장큰값을 제일 오른쪽에 이동되게하라.
	// => a[] {3,6,1,5,7}

	size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] < a[i + 1]) {
				int temp{ a[i] };
				a[i] = a[i + 1];
				a[i + 1] = temp;
			}
		}
	}


	//a는 내림차순으로 정렬되었다.
	cout << "정렬 후: " << endl << endl << endl;
	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:33:31 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include<algorithm>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,1000 };

int main()
{
	int a[1000];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	sort(begin(a), end(a)); //훨씬 빠르다!

	//a는 오름차순으로 정렬되었다.
	cout << "정렬 후: " << endl << endl << endl;
	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:35:32 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include<algorithm>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,1000 };

int main()
{
	int a[1000];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	for (int num : a)
		cout << num << ' ';

	cout << endl;

	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
		}); //훨씬 빠르다!

	//a는 내림차순으로 정렬되었다.
	cout << "정렬 후: " << endl << endl << endl;
	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:38:54 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,1000 };

int main()
{
	int a[100];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] > a[i + 1]) {
				swap(a[i], a[i + 1]);
			}
		}
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:41:43 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

default_random_engine dre{ 2020180777 };
uniform_int_distribution<> uid{ 0,1000 };

int main()
{
	int a[100];

	for (int& num : a) // &로 a배열의 값을 원격조작가능해짐
		num = uid(dre);

	size_t num = sizeof(a) / sizeof(int);

	for(int j = 0; j < num - 1;++j)
	{
		for (int i = 0; i < num - 1 - j; ++i) {
			if (a[i] > a[i + 1]) {
				swap(a[i], a[i + 1]);
				//이건 실제 함수를 호출하지 않고
				//함수의 내용을 '컴파일러'가 여기에 적는다. (이전에 실제코드를 여기에 쓴것처럼)
				//함수를 "인라인" 한다는 말이다.
			}
		}
	}

	for (int n : a)
		cout << n << ' ';

	cout << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:43:50 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
// Lamda 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	[](int n) {
		cout << "안녕? 난 람다라고 해!" << endl;
	};	//이름이 없어 부를수가 없다. => 부를 일이 없는 함수

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:45:06 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 정렬 - 버블 정렬
// sort() 함수
// Lamda 함수
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	[](int n) {
		cout << "안녕? 난 람다라고 해!" << endl;
	};	//이름이 없어 부를수가 없다. => 부를 일이 없는 함수

	printf;

	printf("Hello?"); //여기서 ()는 함수호출 연산자라 부른다.

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:47:10 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	[]() {
		cout << "안녕? 난 람다라고 해!" << endl;
	}();	

	printf;

	printf("Hello?"); //여기서 ()는 함수호출 연산자라 부른다.

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:47:29 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	[]() {
		cout << "안녕? 난 람다라고 해!" << endl;
	}();	//부를수 있게 만들어줌

	printf;

	printf("Hello?"); //여기서 ()는 함수호출 연산자라 부른다.

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 8:48:12 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <random>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	[]() {
		cout << "안녕? 난 람다라고 해!" << endl;
	}();	//부를수 있게 만들어줌(예외적으로)

	printf;

	printf("Hello?"); //여기서 ()는 함수호출 연산자라 부른다.

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 9:04:55 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	sort(begin(str), end(str)-1);

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 9:07:16 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	sort(begin(str), end(str) - 1, [](char a, char b) {
		return a > b;
		});

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-09-27 오후 9:07:28 - (일요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(4주1일)
//
//[9월 28일]1차 시험!
//
// 처리할 데이터수가 점점 늘어난다.
//어떻게?
//어디에 읽어오지?
//몇개씩 처리할 수 있지?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	sort(begin(str), end(str), [](char a, char b) {
		return a > b;
		});

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 6:43:50 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	sort(begin(str), end(str), [](char a, char b) {
		return a > b;
		});

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 6:51:50 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	cout << str << endl;

	sort(begin(str), end(str));

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 6:53:18 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	cout << str << endl;

	sort(begin(str), end(str));

	cout << str << endl; //정렬된후에 널포인터가 맨앞에있어 cout은 널포인터를 만나면 끝나기때문에 아무것도 안보이는것

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 6:53:44 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	cout << str << endl;

	sort(begin(str), end(str));

	cout << str+1 << endl; //널포인터 건너뛰기

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 6:56:14 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//이름없는 함수 람다(lamda)
int main()
{
	char str[]{ "C++ is a nice programming language" };

	cout << str << endl;

	//내림차순을 하고싶다.
	sort(begin(str), end(str)/*함수() ~ */);

	cout << str + 1 << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 7:16:24 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//글짜 비교하기위하 함수
bool 내림차순(char, char);

bool 내림차순(char a, char b) {
	if (a > b)
		return true;
	else
		return false;
}

int main()
{
	char str[]{ "C++ is a nice programming language" };

	cout << str << endl;

	//내림차순을 하고싶다.
	sort(begin(str), end(str), 내림차순 );	//n=log(n)

	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 9:34:45 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//글짜 비교하기위하 함수
bool 내림차순(char, char);

bool 내림차순(char a, char b) {	
	cout << a << ", " << b << endl;
	return a > b;
}

int main()
{
	char str[]{ "0123456789" };

	cout << str << endl;

	//내림차순을 하고싶다.
	sort(begin(str), end(str), 내림차순 );	//n=log(n)
										
	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 9:37:18 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

//글짜 비교하기위한 함수
bool 내림차순(char, char);

bool 내림차순(char a, char b) {	
	static int num = 0;
	cout << a << ", " << b << "  " << num++ << "번째" << endl;
	return a > b;
}

int main()
{
	char str[]{ "0123456789" };

	cout << str << endl;

	//내림차순을 하고싶다.
	sort(begin(str), end(str), 내림차순 );	//n=log(n)
										
	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 9:40:22 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

int main()
{
	char str[]{ "0123456789" };

	cout << str << endl;

	//내림차순을 하고싶다.
	//근데 이 함수를 궂이 생성하고 불러와야하는가? => 람다(이름없는)함수를 이용하자!! - 기호로는 []이다.
	sort(begin(str), end(str), [](char a, char b) {
		return a > b;
		});	//n=log(n)
										
	cout << str << endl;

	save("소스.cpp");
}



----------------------------------------------
소스.cpp - 2020-10-05 오후 9:43:04 - (월요일)
----------------------------------------------

//----------------------------------------------------------------------------------
//2020.2학기 C++															(5주1일)
//
//처리해야하는 자료의 수가 점점 늘어난다. 
//어떻게 하지?
//어디에 읽어와야하나?
//몇개까지 처리가능?
//----------------------------------------------------------------------------------

#pragma warning(disable:4996)
#include <iostream>	
#include <algorithm>
#include "save.h"	
using namespace std;

int main()
{
	char str[]{ "0123456789" };

	cout << str << endl;

	//내림차순을 하고싶다.
	//근데 이 함수를 굳이 생성하고 불러와야하는가?(함수 호출 오버헤드가 너무 과하다!) => 람다(이름없는)함수를 이용하자!! - 기호로는 []이다.
	sort(begin(str), end(str), [](char a, char b) {
		return a > b;
		});	
	// 장점!) 1. 가독성이 좋다.
	//		  2. 함수 인라인화(함수호출오버헤드를 없애서 빠른 구동가능)
										
	cout << str << endl;

	save("소스.cpp");
}
